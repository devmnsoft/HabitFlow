(() => { document.querySelectorAll('.notification-actions form').forEach(form=>form.addEventListener('submit',()=>{const button=form.querySelector('button');button.disabled=true;})); let timer; const refresh=async()=>{if(document.hidden)return;try{const response=await fetch('/notifications/unread-count',{headers:{Accept:'application/json'}});if(!response.ok)return;const data=await response.json();document.querySelectorAll('[data-notification-count]').forEach(b=>{b.textContent=String(data.count);b.hidden=data.count===0;});}catch{ /* navegação permanece disponível sem atualização dinâmica */ }}; const start=()=>{clearInterval(timer);timer=setInterval(refresh,60000);};document.addEventListener('visibilitychange',()=>{if(!document.hidden)refresh();});start();refresh();})();
