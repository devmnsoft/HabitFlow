(() => { const form=document.querySelector('[data-reminder-form]'); if(!form)return; const preview=form.querySelector('[data-schedule-preview]'); const update=()=>{const time=form.querySelector('[name="ReminderTime"]').value; const days=[...form.querySelectorAll('[name="Days"]:checked')].map(x=>x.nextElementSibling.textContent); preview.textContent=time&&days.length?`Próxima programação: ${days.join(', ')}, às ${time}.`:'Escolha ao menos um dia e um horário.';}; form.addEventListener('change',update); form.addEventListener('submit',()=>{const b=form.querySelector('[data-submit]');b.disabled=true;b.textContent='Salvando…';}); update(); })();
