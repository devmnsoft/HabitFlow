<header class="hf-page-header"><div><p class="hf-eyebrow">@ViewData["Eyebrow"]</p><h1>@ViewData["Title"]</h1><p>@ViewData["Subtitle"]</p></div>@if (ViewData["ActionHref"] is string href && ViewData["ActionText"] is string text){<a class="btn btn-success btn-lg" href="@href">@text</a>}</header>
