# Calendário de consistência
O calendário representa previstos, concluídos e percentual com texto, `aria-label` e foco por teclado, nunca apenas por cor. `full_history` decide o período; sem ele, a janela é de 30 dias.
