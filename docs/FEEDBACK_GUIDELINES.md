# Diretrizes de feedback

Use toast para sucesso e informação leve; modal para erro importante ou banco; validação inline para campos. Não use `alert()` ou `confirm()`; prefira `HabitFlowFeedback` e `_FeedbackBridge`.
