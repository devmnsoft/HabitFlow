# Relatórios avançados
`basic_reports`, `advanced_reports`, `report_export_csv`, `report_print`, `full_history` e `consolidated_reports` são avaliados pelo plano efetivo. Insights são regras determinísticas e sempre informam sua base. CSV deve prefixar células iniciadas por `=`, `+`, `-` ou `@`.
