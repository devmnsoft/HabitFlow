# QA funcional v6.8.1

Validar via WebApplicationFactory: login/acesso/logout do SuperAdmin, invalidação após reset, `GET /dashboard`, ambos POSTs AJAX com antiforgery e `GET /progress/calendar`. Comparar isolamento, payload e calendário. Playwright deve confirmar clique duplo, loading, atualização sem reload e `aria-live`.
