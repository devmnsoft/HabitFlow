# PostgreSQL 28P01

`28P01` indica senha inválida. Em Development, revise `Username` e `Password` em `appsettings.Development.local.json`, teste com `psql`, reinicie a aplicação e acesse `/health/db` ou `/diagnostics/database`.
