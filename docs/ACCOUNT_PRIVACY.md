# Privacidade da conta
Hábitos começam como `Private`. `AggregateOnly` permite somente números agrupados e `SharedWithRoutine` permite o contexto escolhido. SuperAdmin recebe métricas operacionais agregadas, nunca conteúdo íntimo por padrão.
