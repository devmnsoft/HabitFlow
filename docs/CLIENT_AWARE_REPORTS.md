# Relatórios conscientes de conta

Todo contrato público novo deve receber `clientId` e `userId`; ausência de contexto explícito é proibida. Relatórios devem consumir `ProgressPeriodSnapshot`, usar semana iniciada na segunda-feira e nunca consultar por pessoa isoladamente. A migração dos contratos legados permanece pendente.
