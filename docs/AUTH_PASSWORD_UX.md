# UX de senha

Campos de senha começam com `type="password"`, usam o partial `_PasswordInput.cshtml`, autocomplete correto e botão `data-password-toggle`. O JavaScript Vanilla alterna visibilidade, `aria-label` e classe visual. No cadastro, `Password` e `ConfirmPassword` são validados no cliente e no backend.
