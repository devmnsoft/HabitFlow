# Migração de planos

Premium mensal/anual é convertido em produto `ritmo` com ciclo `Monthly`/`Yearly`; Enterprise vira `evolucao`; Free vira `free`. Colunas antigas não são removidas e estão documentadas como legadas. A remoção só poderá ocorrer depois de reconciliação e validação em produção.
