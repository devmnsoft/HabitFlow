{
  "name": "habitflow-functions",
  "version": "1.6.0",
  "private": true,
  "description": "Firebase Functions backend do HabitFlow para checkout Premium, webhooks e administração segura.",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "lint": "node --check index.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.2"
  },
  "devDependencies": {}
}
