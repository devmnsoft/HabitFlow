# Pré-requisitos Windows
Instale .NET Hosting Bundle/ASP.NET Core Runtime compatível, IIS com ASP.NET Core Module V2, PostgreSQL 16 com `psql`, `pg_dump` e `pg_restore` no PATH. URL Rewrite é opcional. Configure HTTPS antes de usar cookies seguros em produção.
