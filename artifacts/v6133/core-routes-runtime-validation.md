# Rotas centrais — v6.13.3

**Não executadas.** Startup, autenticação, status HTTP, ações, reload, persistência e logs das rotas centrais permaneceram bloqueados pela ausência do SDK .NET e PostgreSQL. Nenhuma rota é declarada aprovada.
