# Hábitos em runtime — v6.13.3

**Não validado em runtime.** Sem aplicação executável, não foi possível autenticar, abrir `/habits` e suas rotas ou executar o CRUD. A auditoria estática confirmou que `_HabitStatusBadge.cshtml` existe e que card/detalhe usam `Partials/_HabitStatusBadge`; isso não substitui a abertura das rotas.
