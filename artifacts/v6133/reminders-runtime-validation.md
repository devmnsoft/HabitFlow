# Lembretes em runtime — v6.13.3

**Não validado em runtime.** Sem .NET/PostgreSQL não foi possível abrir `/reminders`, materializar `HabitReminderRow` nem executar criar/pausar/reativar/adiar/excluir. A inspeção confirmou DTO, aliases, array `integer[]`, conversões UTC e escopo client/user; nenhuma execução é alegada.
