# Startup e rotas públicas — v6.13.3

**Status: bloqueado, não aprovado.** A aplicação não pôde ser iniciada porque o SDK/runtime .NET não existe no container e seu download foi bloqueado por HTTP 403. Consequentemente `/`, `/plans`, `/login`, `/register`, `/service-worker.js` e `/favicon.ico` não foram abertas nesta execução.
