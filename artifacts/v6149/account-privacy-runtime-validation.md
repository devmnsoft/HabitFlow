# Central de Privacidade — validação runtime

**Status: não executada — P0 pendente.**

A rota autenticada `/account/privacy`, a gravação de `analytics` e `communications` e a persistência após reload exigem aplicação e PostgreSQL reais. O ambiente não oferece `dotnet`, `pwsh`, `psql`, credencial de banco ou sessão autenticada; portanto, esses critérios permanecem pendentes.
