# Migrations e sanidade PostgreSQL — v6.14.5

**P0 pendente:** não executadas. `pwsh` e `psql` estão ausentes e nenhuma connection string real foi fornecida. Banco novo, banco existente, rerun, sanidades e seeds permanecem sem evidência runtime.
