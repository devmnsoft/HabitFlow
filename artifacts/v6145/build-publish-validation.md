# Build e publish — v6.14.5

**P0 pendente:** não executados porque `dotnet` não existe no ambiente (exit 127). Assim, `clean`, `restore`, `build` e `publish` não são declarados aprovados. Nenhum diretório de publish ou binário foi produzido/commitado.
