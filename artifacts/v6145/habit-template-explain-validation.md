# EXPLAIN de templates/favoritos — v6.14.5

**P0 pendente:** nenhum EXPLAIN real foi executado porque `psql` e PostgreSQL não estão disponíveis. A inspeção estática confirmou a construção via `WithClause`, mas não substitui validação do parser, tipos reais ou plano do banco.
