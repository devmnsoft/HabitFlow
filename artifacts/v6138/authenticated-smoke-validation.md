# Smoke autenticado — v6.13.8

Status: **P0 pendente**. O helper existente cobre as rotas exigidas e reload, mas não foi executado neste host: não há runtime .NET/PostgreSQL nem credencial efêmera provisionada. Nenhuma senha foi persistida. A execução autenticada deve ser anexada antes de aprovar a release.
