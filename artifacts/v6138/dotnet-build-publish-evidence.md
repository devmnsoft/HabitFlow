# Build e publish — v6.13.8

Os comandos Release estão no job `dotnet-build-publish`: `dotnet --info`, restore, build com `--no-restore` e publish com `--no-build`.

Status: **pendente de execução no GitHub Actions**. O host Codex não possui `dotnet`; nenhum resultado foi inferido ou aprovado. O diretório publicado será apenas artifact do run e não será commitado.
