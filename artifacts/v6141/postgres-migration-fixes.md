# Migrations PostgreSQL

A validação não foi executada localmente porque `pwsh` e `psql` não estão instalados; o run remoto também ficou inacessível por ausência de autenticação GitHub. Não houve alteração em migration, seed de produto, preço ou plano. P0 pendente de execução real no serviço PostgreSQL do gate.
