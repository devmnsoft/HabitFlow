# Build e publish

O container não fornece o SDK `dotnet`; build e publish não puderam ser executados localmente. O workflow existente continua sendo a fonte de validação, sem supressão de warnings ou remoção de código. Não foi observada nem alegada falha de compilação sem logs reais.
