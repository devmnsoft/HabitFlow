# Frontend e segurança

`npm ci`, `npm run security:scan`, `npm test`, `npm audit --omit=dev` e os nove comandos `node --check` passaram localmente. O audit reportou zero vulnerabilidades. Nenhuma regra ou scanner foi desabilitado e nenhuma correção de frontend foi necessária.
